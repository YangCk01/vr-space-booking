# VR 大空间预约排场系统上下文

## 项目定位

本项目是面向 VR 大空间体验场馆的预约排场与运营管理系统，包含：

- B 端管理后台：运营、店长、财务、管理员使用。
- C 端预约前端：会员或散客进行预约、支付、改签和查看权益。
- 后端服务：订单、会员、财务、审批、对账、营销、设备日志等业务 API。

## 当前运行环境约定

- 开发服使用 `dev.bat` 启动。
- 开发后端端口：`4001`。
- 开发管理端端口：`5175`。
- 开发预约端端口：`5176`。
- 开发数据库：`vrspace_dev`。
- 测试服使用 `test.bat` 启动。
- 同步到测试服使用 `sync-to-test.bat`。

除非用户明确要求，不要把开发服改动同步到测试服。

## 核心业务模块

### 订单与预约

订单围绕预约时段产生。用户选择场地、日期、时段、游戏和人数后创建订单。未支付订单有支付倒计时，超时应自动关闭。已支付订单根据业务状态进入待核销、游戏中、已核销、已完成、已取消、已退款、已作废或爽约等状态。

### 核销

核销表示顾客到店并完成入场确认。一般只有到达开场前允许入场的时间窗口后，订单才应该进入待核销。手动标记爽约再撤销时，不能无条件回到待核销，必须重新按预约时间和业务规则判断。

### 改签

改签是对已预约订单更换日期或时段。会员可能拥有每月免费改签次数。超过免费权益后，可能按订单金额比例收取改签手续费。

### 爽约

爽约表示顾客未在允许宽限期内到场。爽约可能扣除罚金或不退还部分金额。撤销爽约必须保留审计记录，并按时间规则恢复到合理状态。

### 退款与作废

退款是资金处置行为，必须有权限控制、审批或审计记录。已作废订单不应随意直接退款；应根据原支付状态、退款原因、退款金额、是否冲回爽约罚金等字段形成处置记录。

### 会员权益

会员等级影响折扣、免费改签次数等权益。会员权益应由真实业务配置统一驱动，C 端展示、订单计费和后台配置必须保持一致。

### 积分与优惠券

积分可通过消费获得，也可能由管理员或营销活动赠送。积分可用于抵扣或积分商城兑换。优惠券包括折扣券、体验券等，来源可能是人工赠送、活动发放或积分兑换。赠送上限属于风控规则，应清晰区分全局日上限和单用户日上限。

### 财务与对账

财务管理包含收支概览、流水明细、退款记录、日结报表、对账中心和设备日志。对账应能发现系统账、业务账、支付流水、退款流水、积分、券、设备日志之间的差异，并提供定位、人工核实、调整单和审计记录。

### 审批

高风险操作应进入审批中心，例如退款、撤销爽约、财务调整、异常对账处置等。普通操作员可以发起，管理员、店长或财务进行确认。

## 重要实现约定

- 文档尽量使用中文。
- 开发服和测试服互不影响。
- 业务配置不应集中暴露为技术参数页，应按业务模块归属：
  - 会员规则：会员营销 / 等级与权益。
  - 积分规则：会员营销 / 积分规则。
  - 券赠送上限：会员营销或营销活动。
  - 对账告警：财务管理 / 对账中心 / 对账配置。
- 系统健康独立页面已取消；后台健康检查能力可服务于对账、审计或通知。

## 常用术语

- B 端：管理后台。
- C 端：用户预约前端。
- 待核销：顾客可入场确认的订单状态。
- 核销：确认顾客已到店入场。
- 爽约：顾客未按约到场。
- 作废：订单业务上无效，通常不等同于退款。
- 对账异常：系统账与业务账、支付账或设备日志存在差异。
- 调整单：为修正已核实差异而产生的财务或账户调整记录。

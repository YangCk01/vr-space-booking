# 需求文档 (PRD)：门店财务与会员核算系统重构

## 1. 产品概述与核心目标
当前系统将会员储值本金与赠送虚拟金额混同管理，存在引发财务坏账和门店利润误判的高风险。本次重构的核心目标是：**实现“资金流（收付实现制）”与“业务流（权责发生制）”的彻底解耦**。
通过重构底层账户模型与扣款算法，确保每一笔交易账实相符，杜绝财务漏洞，并输出精准的单店盈利报表。

---

## 2. 数据库架构：双钱包模型 (Dual-Wallet)
必须废除原有的单一 `balance`（余额）字段，用户实体下的资金账户必须在物理层拆分为两个独立字段/表进行管理。

| 钱包类型 | 数据库字段定义 (参考) | 财务属性 | 业务规则限制 |
| :--- | :--- | :--- | :--- |
| **本金钱包** | `principal_balance` | 预收账款（负债） | 来源于微信/支付宝/现金真实支付。支持申请退款。 |
| **赠送钱包** | `bonus_balance` | 虚拟促销折让（非资产） | 来源于系统规则赠送。**绝对禁止退款或提现**，随本金退款时自动清零。 |
| **积分账户** | `points_balance` | 递延收益 | 仅由“本金钱包”的消耗产生。仅作现金抵扣，不支持提现。 |

*注：前端 UI 面向用户展示的“总余额” = `principal_balance` + `bonus_balance`。但在后端接口结算时，必须分别调用两个字段。*

---

## 3. 核心核算算法 (Core Algorithms)

### 3.1 消费扣款：等比扣除算法 (Proportional Deduction)
当用户使用余额支付订单时，系统必须按照用户当前账户中本金与赠送的比例，按同等比例分别划扣两个钱包的金额。

**前置计算：本金系数 (Principal Ratio)**
$$\text{本金系数} = \frac{\text{当前本金余额}}{\text{当前本金余额} + \text{当前赠送余额}}$$

**划扣计算逻辑：**
假设订单最终结算金额为 $Total$，则：
- 本金钱包扣减金额 = $Total \times \text{本金系数}$
- 赠送钱包扣减金额 = $Total \times (1 - \text{本金系数})$
- 积分生成数值 = \text{本金钱包扣减金额} \times \text{系统后台配置的积分返还率}

**技术要求：** 扣减过程必须在一个事务 (Transaction) 中完成，确保两个钱包的扣减和积分的生成保持强一致性。小数精度保留到小数点后两位（四舍五入）。

### 3.2 退款清算算法 (Refund Clearing)
当用户发起“会员卡退款”请求时，废除“按余额直接退”的逻辑。必须追溯用户已享受的折扣，按无折扣原价重新审计。

**清算公式：**
$$\text{应退本金} = \text{该用户历史累计充值本金总额} - \sum (\text{已核销总场次} \times \text{单场散客原价})$$

**清算执行动作：**
1. 校验 $\text{应退本金}$，若 $\le 0$，则退款金额为 0 元。
2. 触发支付网关退款接口，按原路退回 $\text{应退本金}$。
3. 将该用户的 `principal_balance`（本金钱包）、`bonus_balance`（赠送钱包）和剩余 `points_balance`（积分）**全部清零**。

### 3.3 积分抵现财务销账规则
积分在支付结算环节抵扣现金时，抵扣掉的部分不计入“营业收入”，视作系统折扣。
- **业务流记录：** 订单总价 150元，积分抵扣 10元，实付/余额划扣 140元。
- **财务流记录：** 确认实际营业收入 140元，记录积分折让成本 10元。

---

## 4. 业务边界与互斥规则 (System Constraints)
开发在处理结算中心 (Checkout Center) 模块时，需严格执行以下强拦截逻辑：
1. **折扣互斥拦截：** 读取到用户身份为会员并享受会员折扣（如 9折）时，前端必须将早鸟票、美团券、节日促销等其他营销活动的选项灰置或隐藏。同一笔订单，折扣类权益仅可生效其一。
2. **积分抵扣上限拦截：** 判定 `本次使用的积分抵扣金额 / 订单总金额`，若该比例大于后台配置的 `单笔最高抵扣比例(%)`（例如 30%），则阻断提交并抛出错误提示。

---

## 5. 核心财务报表产出要求 (Financial Reports)
后端需每日定时跑批 (Batch Job)，统计底层流水表，自动生成并覆盖更新以下三个维度的财务看板数据。

### 5.1 每日现金解缴表 (Cash Flow Report)
- **计算口径：** 纯收付实现制。只看当天银行账户的钱变动。
- **数据指标：**
    - 当日新增充值本金 (只统计 `principal_balance` 的增量)
    - 当日散客直付现金
    - 当日退款流出现金
    - **当日净现金流入** = 充值本金 + 散客直付 - 退款流出

### 5.2 每日确权营收表 (Recognized Revenue Report)
- **计算口径：** 权责发生制。体现门店真实产值。
- **数据指标：**
    - 当日散客核销确权收入
    - 当日会员消费本金划扣确权 (利用等比扣除算法中得出的“本金钱包扣减金额”汇总)
    - **当日总确权营业额** = 散客核销确权 + 会员本金划扣确权

### 5.3 门店负债存量监控台 (Liability Dashboard)
- **计算口径：** 实时查询大盘账户存量。
- **数据指标：**
    - 门店总真实负债 (当前所有用户的 `principal_balance` 总和)
    - 门店虚拟负债池 (当前所有用户的 `bonus_balance` 总和)
    - 沉睡本金规模 (超过 90 天无流水变动的 `principal_balance` 总和)

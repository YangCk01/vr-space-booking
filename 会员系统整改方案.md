# 会员系统整改方案

## 一、现状分析

### 1.1 已实现的部分

| 模块 | 状态 | 说明 |
|------|------|------|
| B端会员营销设置 | ✅ UI+存储 | 可编辑充值档位、会员等级折扣、积分规则，已保存到数据库 |
| C端会员中心 | ✅ 基础展示 | Profile 页展示余额、会员等级（硬编码映射） |
| C端充值页面 | ✅ 基础功能 | Recharge 页支持微信/支付宝充值，但使用硬编码配置 |
| B端会员管理 | ✅ 基础管理 | Users 页展示用户列表、KPI统计、等级筛选 |
| 后端充值记录 | ✅ 完整 | RechargeRecord + BalanceTransaction 模型及CRUD |
| 会员等级自动升级 | ✅ 部分 | 充值成功后按单次充值金额升级（硬编码逻辑） |

### 1.2 未实现/脱节的部分

| 模块 | 状态 | 问题 |
|------|------|------|
| **充值配置联动** | ❌ | B端设置 `recharge_tiers` 保存在数据库，但后端 `rechargeController` 使用**硬编码** `RECHARGE_CONFIG`，C端展示与B端配置完全脱节 |
| **会员折扣应用** | ❌ | B端设置 `member_levels` 含折扣字段，但订单创建/支付流程**从未读取和应用折扣** |
| **积分系统** | ❌ | B端可设置积分规则，但 `User` 表**无 `points` 字段**，订单/充值**不计算积分**，C端**不展示积分** |
| **C端等级展示** | ⚠️ | Profile 页等级名称是**硬编码映射**（NORMAL→普通用户），不跟随B端设置的可编辑等级名称 |
| **C端充值页** | ⚠️ | 充值档位卡片是后端硬编码返回，**不反映B端设置**；页面**不展示当前等级折扣** |
| **会员管理信息展示** | ⚠️ | 用户详情/编辑页缺少：当前等级折扣、积分余额、充值历史、累计积分等关键信息 |

---

## 二、整改目标

**核心原则**：B端配置即真理。所有C端展示、后端业务逻辑必须读取B端 `SystemSetting` 中的配置，实现"配置驱动"。

### 2.1 目标总览

1. **B端配置 → 驱动 C端展示**：C端会员中心、充值页展示的内容与B端设置完全一致
2. **B端配置 → 驱动后端业务**：充值赠送金额、会员折扣、积分计算全部读取数据库配置
3. **会员管理信息完善**：B端用户详情展示更完整的会员信息
4. **积分系统完整落地**：从"UI摆设"变成真正可使用的功能

---

## 三、具体整改内容

### 3.1 数据库层改动

#### 3.1.1 User 表增加 `points` 字段
```prisma
model User {
  // ... existing fields
  points        Int       @default(0)   // 新增：积分余额
}
```

#### 3.1.2 数据迁移
- 现有用户 `points` 初始化为 `0`
- 已有历史订单可补算积分（可选，按需执行）

---

### 3.2 后端层改动

#### 3.2.1 重构 `rechargeController.ts`

**当前问题**：`RECHARGE_CONFIG` 是写死的数组：
```ts
const RECHARGE_CONFIG = [
  { amount: 50000, bonus: 10000, level: 'NORMAL' },
  { amount: 100000, bonus: 30000, level: 'MEMBER' },
  ...
]
```

**整改方案**：
1. 新增辅助函数 `getRechargeConfig()`，从 `SystemSetting` 表读取 `recharge_tiers` 配置
2. 将硬编码的 `RECHARGE_CONFIG` 替换为动态读取的配置
3. `GET /recharges/config` 返回B端设置的档位（金额单位从"元"自动转"分"）
4. `confirm()` 中的等级升级逻辑也使用配置数据（而非硬编码金额判断）

**关键代码变更**：
```ts
async function getRechargeConfig() {
  const setting = await prisma.systemSetting.findUnique({ where: { key: 'recharge_tiers' } })
  const tiers = (setting?.value as any)?.value || []
  return tiers.map((t: any) => ({
    amount: t.amount * 100,   // 元 → 分
    bonus: t.bonus * 100,     // 元 → 分
    level: t.level,           // 等级 key
  }))
}
```

#### 3.2.2 新增 `GET /settings/member` 公共接口

**当前问题**：C端需要展示会员等级名称、折扣、积分规则，但 `/settings` 接口可能需要管理员权限。

**整改方案**：新增一个**公开**（或客户鉴权）接口，返回会员相关配置：
```ts
GET /settings/member-public
返回：
{
  levels: [{ key, name, discount, threshold }],
  points: { earnRate, deductRate }
}
```

#### 3.2.3 订单支付流程应用会员折扣

**当前问题**：`orderController.create()` 和 `payOrder()` 完全不读取 `member_levels` 折扣配置。

**整改方案**：
1. 新增辅助函数 `getMemberDiscount(userLevel)`，读取 `member_levels` 配置返回折扣率
2. 在订单创建时（`create()`），根据用户当前等级计算折扣后金额：
   ```ts
   const discount = await getMemberDiscount(user.level) // e.g. 95 → 0.95
   const finalAmount = Math.round(originalAmount * discount / 100)
   ```
3. 订单金额显示原价 + 折扣价，便于用户感知会员权益
4. **注意**：余额扣款时也要按折扣后金额扣除

#### 3.2.4 积分系统完整实现

**当前问题**：积分规则设置存在，但无任何业务逻辑。

**整改方案**：

**(1) 积分获取 — 消费时**
- 在订单支付成功（`payOrder` 或 `create` with BALANCE）后：
  ```ts
  const earnRate = await getPointsEarnRate()  // 读取 settings
  const earnedPoints = Math.floor(order.amount / 100 * earnRate)  // 按元计算
  await tx.user.update({ where: { id: userId }, data: { points: { increment: earnedPoints } } })
  ```

**(2) 积分获取 — 充值时**
- 在充值成功（`rechargeController.confirm`）后，可按同样的规则赠送积分（或额外规则）

**(3) 积分抵扣 — 下单时**
- 在 `OrderConfirm` 流程中，允许用户使用积分抵扣部分金额
- 后端校验：`deductPoints / deductRate` 为可抵扣金额（元）
- 抵扣后金额 = 原金额 - 抵扣金额
- 扣除用户积分，创建积分抵扣记录（可记入 `BalanceTransaction` remark）

**(4) 积分变动记录**
- 可复用 `BalanceTransaction` 表，用 `type: 'POINTS_EARN'` / `'POINTS_DEDUCT'` 记录
- 或新增 `PointsTransaction` 表（推荐，更清晰）

#### 3.2.5 会员等级升级逻辑优化

**当前问题**：等级升级只基于单次充值金额，且硬编码判断。

**整改方案**：
1. 升级逻辑改为读取 `recharge_tiers` 配置
2. 充值成功后，匹配当前充值金额对应的档位，取该档位的 `level`
3. 如果目标等级高于当前等级，则升级
4. 支持**降级**（可选，如果用户要求的话）：可基于累计消费/充值金额判断

#### 3.2.6 用户详情接口增加会员信息

**当前问题**：`GET /users/:id` 返回的字段较基础。

**整改方案**：用户详情接口增加：
- `points`（积分余额）
- `currentDiscount`（当前等级折扣率）
- `rechargeHistory`（最近充值记录，可选）
- `totalRecharged`（累计充值金额）

---

### 3.3 C端（Customer Frontend）改动

#### 3.3.1 会员中心页 (`Profile.tsx`) 整改

**当前状态**：
- 余额、等级名称（硬编码）、菜单列表
- 不展示折扣、积分

**整改后**：
```
┌─────────────────────────────────────┐
│  [头像]  用户名                       │
│  当前等级: 银卡会员  |  享95折         │
├─────────────────────────────────────┤
│  余额: ¥1280  |  积分: 850           │
├─────────────────────────────────────┤
│  [会员储值]  [我的订单]  [积分明细]   │
└─────────────────────────────────────┘
```

**具体改动**：
1. 调用 `GET /settings/member-public` 获取等级配置，动态映射等级名称和折扣
2. 展示当前等级对应的折扣（如"享95折"）
3. 展示积分余额
4. 充值入口旁展示当前最高可充值档位提示

#### 3.3.2 充值页 (`Recharge.tsx`) 整改

**当前状态**：
- 展示硬编码档位卡片
- 不展示等级折扣
- 无积分相关信息

**整改后**：
```
┌─────────────────────────────────────┐
│  当前余额: ¥1280                     │
│  当前等级: 银卡会员 (消费享95折)       │
├─────────────────────────────────────┤
│  选择充值档位:                        │
│  ┌────────┐ ┌────────┐ ┌────────┐  │
│  │ ¥500   │ │ ¥1000  │ │ ¥2000  │  │
│  │ 赠¥0   │ │ 赠¥100 │ │ 赠¥300 │  │
│  │普通会员 │ │银卡会员│ │金卡会员│  │
│  └────────┘ └────────┘ └────────┘  │
│  （档位从B端配置读取）                 │
├─────────────────────────────────────┤
│  充值后可得积分: +XXX 分              │
└─────────────────────────────────────┘
```

**具体改动**：
1. `GET /recharges/config` 改为返回B端配置的档位（后端已改造）
2. 每个档位卡片展示：充值金额、赠送金额、对应等级名称（从 `member-public` 接口读取）
3. 顶部增加当前等级折扣提示
4. 底部增加"充值可得积分"提示（根据积分规则计算）

#### 3.3.3 订单确认页 (`OrderConfirm.tsx`)

**当前状态**：不展示任何会员折扣信息。

**整改后**：
1. 展示当前用户等级和折扣（如"银卡会员享95折"）
2. 展示原价和折扣后价格
3. 若支持积分抵扣，展示"可用XX积分抵扣XX元"选项

---

### 3.4 B端（Admin Frontend）改动

#### 3.4.1 会员营销设置页 (`Settings.tsx`) — 已部分完成

**当前状态**：已合并充值档位和会员等级，可编辑。

**需要补充**：
1. 在保存前增加**数据校验**：
   - 每个等级必须有名称、折扣（0-100）
   - 充值金额、赠送金额不能为负数
   - 等级 key 不能重复
2. 增加**预览提示**：告诉用户当前配置在C端会如何展示

#### 3.4.2 会员管理页 (`Users.tsx`) 整改

**当前状态**：
- KPI：用户总数、普通用户、会员用户、VIP用户
- 表格：用户、手机号、等级、注册时间
- 详情：基本信息、累计消费、预约次数

**整改后**：

**(1) KPI 卡片优化**
```
用户总数 | 普通会员 | 银卡会员 | 金卡会员 | 钻石会员
```
- 从硬编码的 `normal/member/vip/vipPlus` 改为**动态读取 `member_levels` 配置**
- 每个等级一张卡片，展示该等级用户数

**(2) 筛选标签优化**
```
全部 | 普通会员 | 银卡会员 | 金卡会员 | 钻石会员
```
- 同样动态读取配置生成标签

**(3) 用户表格增加列**
- 积分余额
- 累计充值
- 当前折扣（如"95折"）

**(4) 用户详情 Sheet 完善**
```
┌────────────────────────────────────────┐
│  [头像] Moro                            │
│  当前等级: VIP+  |  享85折  |  积分: 1250│
├────────────────────────────────────────┤
│  累计消费: ¥3,680  |  累计充值: ¥5,000  │
│  预约次数: 12次  |  最近消费: 2026-05-26 │
├────────────────────────────────────────┤
│  基本信息                               │
│  手机号 / 邮箱 / 注册时间 ...            │
├────────────────────────────────────────┤
│  [充值记录]  [消费记录]  [积分明细]       │
└────────────────────────────────────────┘
```

**具体新增字段**：
- 当前等级折扣
- 积分余额
- 累计充值金额（从 `RechargeRecord` 汇总）
- 充值记录列表（最近5条）
- 积分变动列表（最近5条，可选）

---

## 四、实施步骤（建议顺序）

### Phase 1: 数据库 + 后端基础（1-2天）
1. **数据库迁移**：给 `User` 表加 `points` 字段
2. **后端配置读取**：新增 `getMemberConfig()` / `getRechargeConfig()` 辅助函数
3. **后端公开接口**：新增 `GET /settings/member-public`
4. **后端充值改造**：`rechargeController` 改为读取配置，删除硬编码

### Phase 2: C端会员展示（1天）
5. **C端 Profile 页**：动态读取等级名称/折扣，展示积分
6. **C端 Recharge 页**：动态读取档位配置，展示等级和积分提示
7. **C端 OrderConfirm 页**：展示会员折扣（若Phase 3提前可做抵扣）

### Phase 3: 订单折扣 + 积分系统（2天）
8. **后端订单折扣**：`orderController` 应用会员折扣
9. **后端积分获取**：订单支付成功后赠送积分
10. **后端积分抵扣**：支持下单时使用积分抵扣（API + 校验逻辑）
11. **C端积分抵扣UI**：OrderConfirm 页增加积分抵扣开关/输入

### Phase 4: B端会员管理完善（1天）
12. **B端 Users 页**：KPI动态化、表格加积分/充值列
13. **B端用户详情**：增加折扣、积分、充值记录展示
14. **B端设置校验**：保存前加数据校验和预览提示

### Phase 5: 数据补算（按需）
15. 对历史已支付订单补算积分（一次性脚本）

---

## 五、影响范围与风险评估

### 5.1 影响范围

| 影响面 | 说明 |
|--------|------|
| **数据库** | 仅需给 `User` 表加 `points` 字段，低风险 |
| **现有用户** | 等级、余额不受影响；积分从0开始，可后续补算 |
| **C端用户** | 充值档位展示会变化（从硬编码→B端配置），需要确认B端配置已正确填写 |
| **订单金额** | 新增折扣计算后，订单金额会变化，需测试各种支付场景 |
| **B端管理员** | 会员管理页面展示信息更丰富，操作习惯不变 |

### 5.2 风险与应对

| 风险 | 应对方案 |
|------|----------|
| B端配置为空/错误导致C端展示异常 | 后端读取配置时加 fallback（使用默认配置），C端展示加 loading/错误态 |
| 订单折扣导致金额计算不一致 | 前后端统一计算公式，后端最终校验，防止篡改 |
| 积分抵扣超扣 | 后端严格校验 `user.points >= deductPoints`，事务内扣减 |
| 历史数据兼容 | `User.points` 默认 `0`，不强制补算，可后续按需执行 |

---

## 六、需要确认的事项

1. **积分抵扣比例**：当前设置是 "X积分抵扣1元"（如100积分=1元），是否确认这个规则？
2. **积分获取范围**：只有消费送积分，还是充值也送积分？消费金额按原价还是折后价计算？
3. **等级升级规则**：当前是"单次充值满额升级"，是否需要改为"累计充值/消费满额升级"？
4. **等级降级**：是否支持降级（如消费减少后降级）？
5. **会员管理页 KPI 样式**：是否希望 KPI 卡片数量随等级数量动态变化（可能很多）？

---

*方案撰写时间：2026-05-26*
*待用户确认后按 Phase 分步实施*
